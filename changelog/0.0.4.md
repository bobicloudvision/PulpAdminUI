# 0.0.4

**Released:** 2026-04-15

### Added

- **Distributions page:** new `/distributions/list` management screen with inline edit/delete actions
- **Distributions hook:** `usePulpDistributions` for list refresh and mutation handling
- **Navigation entry:** sidebar link to `Distributions` in the Repository section

### Changed

- **Repository flow:** success panel now includes a direct link to open the distributions page

### Fixed

- **Distribution identifier handling:** update/delete now accept full distribution references instead of RPM-only id parsing
- **Distribution API proxy:** distribution routes now resolve and validate generic `/distributions/...` paths
